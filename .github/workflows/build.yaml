on: push

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v4
    - uses: mlugg/setup-zig@v2
      with:
        version: 0.14.1
    - run: zig build
    - uses: actions/upload-artifact@v4
      with:
        path: zig-out/bin/kime-to
        name: kime-to
